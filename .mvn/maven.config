# Maven Configuration for MCMS Project
# This file contains command-line options passed to all Maven executions

# Show full error traces
-e

# Use 2 threads for parallel builds (adjust based on CPU cores)
#-T 2

# Optional: Offline mode (uncomment to work without internet)
# -o

# Optional: Quiet output (uncomment for less verbose builds)
# -q

# Optional: Debug output (uncomment for troubleshooting)
# -X

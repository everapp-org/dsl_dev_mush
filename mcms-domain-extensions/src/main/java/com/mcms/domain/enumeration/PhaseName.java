package com.mcms.domain.enumeration;

/**
 * Enum representing the 10 phases of the Batch lifecycle.
 * This stub mirrors the JHipster-generated PhaseName enum in mcms-jhipster-base.
 * Used by the generated BatchStateMachine to compile independently.
 */
public enum PhaseName {
    INOCULATION,
    EARLY_COLONIZATION,
    FULL_COLONIZATION,
    CONSOLIDATION,
    FRUITING_TRIGGER,
    PRIMORDIA,
    FRUITING_BODY_GROWTH,
    HARVEST,
    REHYDRATION_PAUSE,
    COMPLETED
}
